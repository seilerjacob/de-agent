# Example: Data Warehouse Design

A standalone SQL example showing a complete data warehouse schema with
seed data — ready to load into DuckDB or any ANSI SQL-compatible database.

## Files
- `schema.sql` — Data Definition Language (DDL) for a small star schema
- `seed_data.sql` — Sample INSERT statements to populate the schema
