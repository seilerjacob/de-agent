# Local development environment variables.
# Copy this file to .env and fill in any real values.
# Currently no secrets are required — this file documents the pattern
# for when credentials are added (e.g., external API keys).

# DBT_PROFILES_DIR=./dbt_project   # set if running dbt outside the Makefile targets
